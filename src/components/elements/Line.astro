---
interface Props {
  width?: string;
}
const { width = "60%" } = Astro.props;
---

<div class="line-sep" style=`--line-width: ${width}`></div>

<style>
  .line-sep {
    width: var(--line-width);
    margin: 4rem auto;
    height: 4px;
    background-color: var(--accent);
  }
</style>
